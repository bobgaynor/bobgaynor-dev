{
  "name": "bobgaynor-dev",
  "version": "0.1.0",
  "description": "Portfolio website deployment environment.",
  "scripts": {
    "build": "echo 'No build step required for static content.'",
    "test": "echo 'No tests configured yet.'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}